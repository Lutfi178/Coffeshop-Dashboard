{
  "name": "brewly-coffee-pos-monorepo",
  "private": true,
  "scripts": {
    "build": "npm run build --prefix frontend",
    "dev": "npm run dev --prefix frontend",
    "lint": "npm run lint --prefix frontend && bun run --cwd backend lint"
  }
}
